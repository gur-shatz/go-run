# ECHO_PORT is passed from the parent runctl.yaml vars
vars:
  LISTEN_ADDR: '0.0.0.0:{{ .ECHO_PORT | default "8081" }}'
watch:
  - "*.sh"
build:
  - /bin/sleep 2
  - echo slept 2
  - /bin/sleep 2
  - echo 'slept 2+2'
exec:
  - sh server.sh
